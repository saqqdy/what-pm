

/**
 * Detects if a project is a workspace project
 */
function isWorkspace() {}

/**
 * Detects if a project is a workspace project sync
 */
function isWorkspaceSync() {}

export { isWorkspaceSync, isWorkspace, isWorkspace as default }
